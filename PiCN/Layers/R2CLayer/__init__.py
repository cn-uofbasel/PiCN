"""Request to Computation Layer. Adding this layer to the stack will enable the program to handle
R2C Messages such as Timeout Prevention. This layer can contain several R2C handler."""

from .BasicR2CLayer import BasicR2CLayer
